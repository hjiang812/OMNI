%% This file sets the basic parameters used in the data generation process

Params.SNR_target = 10; % Signal-to-noise ratio (SNR) of the generated signal

Params.noiseType = 'gaussian'; % Type of noise

Params.numChannels = 1024;

Params.sample_frequency = 30000; % Signal sampling frequency

Params.length = 1; % Signal duration (in seconds)

Params.influenceRange = 30;  % Influence range of a single neuron
